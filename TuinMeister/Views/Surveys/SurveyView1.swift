import SwiftUI

struct SurveyView1: View {
    var body: some View {
        VStack {
            Text("Survey")
                .font(.title)
        }
        .navigationBarBackButtonHidden(true)
    }
}
